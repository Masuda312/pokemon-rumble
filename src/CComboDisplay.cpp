#include "CComboDisplay.hpp"

void CComboDisplay::setComboCount(u32 count) { m_comboCount = count; }

u32 CComboDisplay::getComboCount() { return m_comboCount; }

void CComboDisplay::setComboTimer(u32 timer) { m_comboTimer = timer; }

u32 CComboDisplay::getComboTimer() { return m_comboTimer; }
